import { LigneDeCommandeKey } from './ligne-de-commande-key';

describe('LigneDeCommandeKey', () => {
  it('should create an instance', () => {
    expect(new LigneDeCommandeKey()).toBeTruthy();
  });
});
